{% extends "MainBundle:PageLayout:base_admin.html.twig" %}
{% block javascripts %}
{% endblock %}
{%- set chain = current_chain()-%}
{% block body %}
    <h1>Item edit</h1>

    <form action="{{ path('admin_item_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">Edit</button>
        </p>
    </form>

    <div>
        <a href="{{ path_city('admin_item', {'chain_id':chain.getId}) }}" class="btn btn-primary">К списку</a>
        {#<form action="&#123;&#35;{ path_city('admin_branch_delete', { 'id': entity.id, 'chain_id':chain.getId }) }&#35;&#125;" method="post">#}
        {#{{ form_widget(delete_form) }}#}
        <button type="submit" class="btn btn-danger">Удалить</button>
        {#</form>#}
    </div>
{% endblock %}