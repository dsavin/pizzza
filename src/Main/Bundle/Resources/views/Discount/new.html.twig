{% extends "MainBundle:PageLayout:base_admin.html.twig" %}
{%- set chain = current_chain() -%}
{% block body %}
    <h3>Добавление акции</h3>

    <form action="{{ path_city('admin_discount_new', { 'chain_id': chain.id, 'dis_id':dis_id, '_city':_city}) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn btn-success">Добавить</button>
        </p>
    </form>
    <div>
        <a href="{{ path_city('admin_discount', { 'chain_id': chain.id, '_locale':'ru', '_city':_city  }) }}" class="btn btn-info">К списку</a>
    </div>
{% endblock %}
